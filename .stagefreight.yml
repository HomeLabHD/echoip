# ------------------------------------------------------------------------------
# GENERATED / ENFORCED BY STAGEFREIGHT GOVERNANCE
# Source repo: PrPlanIT/MaintenancePolicy
# Source ref: 215e5003cb5dcfc24c095b549d24bf5010db700b
# Profile: hlhd-docker
# This .stagefreight.yml is governed, not purely local.
# To disable governance, detach from the control repo workflow.
# Manual changes may be overwritten by reconciliation.
# ------------------------------------------------------------------------------

version: 1
vars:
  github_org: HomeLabHD
  github_repo: echoip
  gitlab_group: HomeLabHD
  org: hlhd
  repo: echoip
builds:
  preset: preset/build-docker.yml
scribe:
  preset: preset/scribe-go-docker.yml
lint:
  preset: preset/lint-generic.yml
security:
  preset: preset/security.yml
dependency:
  preset: preset/dependency.yml
commit:
  preset: preset/commit.yml
release:
  preset: preset/release.yml
manifest:
  preset: preset/manifest.yml
build_cache:
  preset: preset/build_cache.yml
ci:
  forge:
    - gitlab
  image: docker.cr.pcfae.com/prplanit/stagefreight:latest-dev
forges:
  preset: preset/forges.yml
git:
  preset: preset/git.yml
metadata:
  category: networking
  description:
    - Self-hosted service for looking up your own public IP address.
    - Container image for echoip, a small IP-address lookup service that reports the caller's public address and geolocation over plain HTTP or JSON, suitable for self-hosting behind a reverse proxy.
  license: BSD-3-Clause
  org: HomeLabHD
  title: echoip
  topics:
    - ip
    - geoip
    - lookup
    - networking
    - self-hosted
orgs:
  HomeLabHD:
    aliases:
      docker: hlhd
      ghcr: homelabhd
      github: HomeLabHD
      handle: hlhd
    maintainer: HomeLabHD <homelabhelp@gmail.com>
preset_source:
  cache_policy: authoritative
  project_id: PrPlanIT/MaintenancePolicy
  provider: gitlab
  ref: 215e5003cb5dcfc24c095b549d24bf5010db700b
  repo_url: https://gitlab.prplanit.com
publish:
  preset: preset/publish-docker.yml
registries:
  preset: preset/registries.yml
repos:
  github-mirror:
    forge: github
    project: HomeLabHD/echoip
    roles:
      - mirror
      - publish-origin
    sync:
      git: true
      releases: true
  primary:
    branches:
      default: main
    forge: gitlab
    project: HomeLabHD/echoip
    roles:
      - primary
    worktree: .
stencils:
  presets:
    - preset/stencils-base.yml
    - preset/stencils-status.yml
    - preset/stencils-docker.yml
    - preset/stencils-go.yml
test:
  preset: preset/test-go.yml
toolchains:
  preset: preset/toolchains.yml
